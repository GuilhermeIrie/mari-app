import { Button, Dialog, DialogActions, DialogTitle } from "@mui/material"
import { Link } from "react-router-dom"

const StyledDialog = ({title, descriptionContent, handleExit,}) => {
    return (
       <div></div>
    )
}